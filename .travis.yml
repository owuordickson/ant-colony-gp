language: python

python:
    - "2.7"
    - "3.6"

before_install:
  - "pip install -U pip"
  - "python setup.py install"

install: pip install -r requirements.txt

script: python algorithms/border_tgraank.py -f DATASET.csv